import tkinter as tk


root = tk.Tk()
root.title("Mijn opdracht GUI")
label = tk.Label(root,text='Naam')
label.grid(padx=0, pady=0)
entry = tk.Entry(root, text='')
entry.grid(padx = 0, pady = 1)
root.mainloop()
